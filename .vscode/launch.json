{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Run Your Script",
        "program": "${workspaceFolder}/node_modules/.bin/pnpm",
        "args": ["run", "pnpm dev"], // Replace "your-script-name" with the actual script name from package.json
        "cwd": "${workspaceFolder}",
      }
    ]
  }
  